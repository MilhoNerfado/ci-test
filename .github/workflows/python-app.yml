# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Code quality

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  teste:
    runs-on: self-hosted

    container: 
      image: zephyrprojectrtos/ci:latest
      volumes:
        - /:/workdir
      options: --user root

    steps:
      - run: |
          pwd
          ls -al /
          echo "-----"
          ls -al /__e
          echo "-----"
          ls -al /__w
          echo "-----"
          ls -al /github
          echo "-----"
          ls -al /home
          echo "-----"
          ls /workdir
          echo "-----"
          cd /workdir
          echo "-----"
          west init -m https://github.com/edgebr/zephyr --mr zephyr_tury
          west update



